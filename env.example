# Database Configuration
# For local development, use a local PostgreSQL instance
# For production (Vercel), use a hosted PostgreSQL service like Neon, Supabase, or PlanetScale

# Connection URL for Prisma (used for migrations and general queries)
DATABASE_URL="postgresql://username:password@localhost:5432/instacircle?schema=public"

# Direct connection URL for Prisma (used for migrations and direct connections)
# This should be the same as DATABASE_URL for most cases, but some providers require different URLs
DIRECT_URL="postgresql://username:password@localhost:5432/instacircle?schema=public"

# Example for Neon (recommended for Vercel):
# DATABASE_URL="postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/instacircle?sslmode=require"
# DIRECT_URL="postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/instacircle?sslmode=require"

# Example for Supabase:
# DATABASE_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres?schema=public"
# DIRECT_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres?schema=public"
